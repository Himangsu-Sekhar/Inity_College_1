<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Inity</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="shortcut icon" href="https://static.vecteezy.com/system/resources/previews/012/488/958/non_2x/bed-icon-vector.jpg" type="image/x-icon">
</head>

<body class="bg-gradient-to-br from-gray-50 to-gray-100 min-h-screen">

  <!-- Navigation -->
  <nav class="bg-gradient-to-r from-blue-600 to-blue-700 shadow-lg">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="flex justify-between items-center h-16">
        <a href="/">
          <div class="flex items-center gap-2">
            <div
              class="w-10 h-10 bg-gradient-to-br from-white-600 to-white-700 rounded-xl flex items-center justify-center">
              <svg class="w-6 h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                  d="M19 21V5a2 2 0 00-2-2H7a2 2 0 00-2 2v16m14 0h2m-2 0h-5m-9 0H3m2 0h5M9 7h1m-1 4h1m4-4h1m-1 4h1m-5 10v-5a1 1 0 011-1h2a1 1 0 011 1v5m-4 0h4">
                </path>
              </svg>
            </div>
            <span class="font-bold text-2xl text-white">Inity</span>
          </div>
        </a>
        <!-- TESTING -->
        
        <!-- TESTING -->
        <div class="flex items-center gap-4">
          <% if (session.userId) { %>
            <span class="text-blue-100 text-sm sm:text-base">Hi, <span class="font-semibold text-white">
                <%= session.userName %>
              </span></span>
            <a href="/logout"
              class="bg-white/10 hover:bg-white/20 text-white px-4 py-2 rounded-lg transition-all duration-200 text-sm font-medium backdrop-blur-sm">
              Logout
            </a>
            <% } else { %>
              <a href="/login"
                class="bg-white text-blue-600 hover:bg-blue-50 px-5 py-2 rounded-lg transition-all duration-200 font-medium shadow-sm">
                Login
              </a>
              <% } %>
        </div>
      </div>
    </div>
  </nav>

  <!-- Main Content -->
  <main class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
    <!-- Success Message -->
    <% if (success && success.length) { %>
      <div class="mb-6 bg-green-50 border-l-4 border-green-500 rounded-r-lg shadow-sm animate-fade-in">
        <div class="flex items-start p-4">
          <svg class="w-6 h-6 text-green-500 mr-3 flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
              d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
          </svg>
          <div class="flex-1">
            <p class="text-green-800 font-medium">
              <%= success %>
            </p>
          </div>
        </div>
      </div>
      <% } %>

        <!-- Error Message -->
        <% if (error && error.length) { %>
          <div class="mb-6 bg-red-50 border-l-4 border-red-500 rounded-r-lg shadow-sm animate-fade-in">
            <div class="flex items-start p-4">
              <svg class="w-6 h-6 text-red-500 mr-3 flex-shrink-0" fill="none" stroke="currentColor"
                viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                  d="M12 8v4m0 4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
              </svg>
              <div class="flex-1">
                <p class="text-red-800 font-medium">
                  <%= error %>
                </p>
              </div>
            </div>
          </div>
          <% } %>

            <!-- Page Content -->
            <div class="bg-white rounded-xl shadow-sm p-6">
              <%- body %>
            </div>
  </main>

  <style>
    @keyframes fade-in {
      from {
        opacity: 0;
        transform: translateY(-10px);
      }

      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    .animate-fade-in {
      animation: fade-in 0.3s ease-out;
    }
  </style>

</body>

</html>